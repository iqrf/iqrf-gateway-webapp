/**
 * RequiredInterface instance interface
 */
interface RequiredInstance {
	instance: string
}

/**
 * RequiredInterface interface
 */
export interface RequiredInterface {
	name: string
	target: RequiredInstance
}
