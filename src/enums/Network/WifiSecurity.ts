/**
 * WEP key length
 */
export enum WepKeyLen {
	BIT64 = '64bit',
	BIT128 = '128bit',
}

/**
 * WEP key type
 */
export enum WepKeyType {
	KEY = 'key',
	PASSPHRASE = 'passphrase',
	UNKNOWN = 'unknown',
}
