/**
 * Embedded Coordinator Set DPA param options
 */
export enum DpaParam {
	/// Last RSSI
	Rssi = 0,
	/// Supply voltage
	Voltage = 1,
	/// System value
	System = 2,
	/// User-specified DPA value
	UserSpecified = 3,
}
