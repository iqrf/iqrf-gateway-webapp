/**
 * Mapping device type
 */
export enum MappingDeviceType {
	/// Adapter
	Adapter = 'adapter',
	/// Board
	Board = 'board',
}

/**
 * Mapping type
 */
export enum MappingType {
	/// USB CDC
	CDC = 'cdc',
	/// SPI
	SPI = 'spi',
	/// UART
	UART = 'uart',
}
