/**
 * Form actions
 */
export enum FormAction {
	/// Add a new resource
	Add = 'add',
	/// Cancel
	Cancel = 'cancel',
	/// Edit the resource
	Edit = 'edit',
	/// Delete the resource
	Delete = 'delete',
}

export enum ServiceAction {
	Enable = 'enable',
	Start = 'start',
	Disable = 'disable',
}
